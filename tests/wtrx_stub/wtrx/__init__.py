"""Stand-in for the 350.org site's ``wtrx`` app, for the contrib.wtrx tests.

Only ``wtrx.blocks`` is mirrored. The real site isn't a dependency of this
repository (#14 asks for repository-owned fixtures).
"""
